# Grype DB snapshot — too large for git (1.4 GB)
logs/db_snapshots/

# OSV-Scanner raw output — not consumed by the pipeline (normalisers/
# only has trivy.py and grype.py; the live osv.py enricher queries
# OSV.dev directly per-CVE, separate from this bulk dump). Can exceed
# GitHub's 100MB file limit on noisy images (php/PHP-app scans
# especially -- one was 263MB).
data/raw/osv/

# SBOMs — regeneratable via Syft; large (up to 33 MB each)
sbom/

# Presentation materials
generate_presentation.py
presentation.pptx

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Editor
.vscode/
*.swp
.claude/

# Locally-installed agent skills (npx skills add), not part of the project
.agents/
skills-lock.json

# The dissertation PDF itself — this repository is public
_MSI__*.pdf
final_delivery.pdf

# Working notes — kept locally, not shipped; the README carries what a
# reader of this package needs, and the report carries the rest
docs/

# Audit write-up — kept locally, not shipped
validation-report.md
